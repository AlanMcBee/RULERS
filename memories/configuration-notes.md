# Configuration Notes

- Configuration for the PowerShell module and ETL integration should be durable and managed through PowerShell where possible.
- SEC-related request settings such as the contact identifier should be configurable rather than hard-coded.
- Prefer environment variables or PowerShell-based configuration helpers over embedded constants in code.
